/**
 * Shared app utilities. Re-exports helpers used across the app.
 * ShadCN `cn` lives in `@/lib/utils` — import from here if you prefer a single utils entry.
 */
export { cn } from "@/lib/utils";
